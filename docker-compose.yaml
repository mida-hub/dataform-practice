services:
  dataform:
    init: true
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dataform
    volumes:
      - ./:/usr/app/dataform
      - ~/.config/gcloud:/root/.config/gcloud
